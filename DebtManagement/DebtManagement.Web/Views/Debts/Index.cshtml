@model DebtManagement.Web.DTOs.DebtsViewModel

@{
    ViewData["Title"] = "Debts";
}

<h1>Debts</h1>

<p>
    <a asp-action="Create" class="btn btn-primary">Create New</a>
</p>

@* Credit Card *@
@if (Model == null || !Model.CreditCardDebts.Any())
{
    <p>No credit card debts available.</p>
}
else
{
    <h2>Credit Card Debts</h2>
    <table class="table">
        <thead>
            <tr>
                <th>Debt Type</th>
                <th>Debt Amount</th>
                <th>Installments</th>
                <th>Remaining Amount</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        @foreach (var item in Model.CreditCardDebts)
        {
            <tr>
                <td>@item.DebtType</td>
                <td>@item.DebtAmount</td>
                <td>@item.PaidInstallments/@item.Installments</td> @* Updated for paid/total format *@
                <td>@item.RemainingAmount</td>
                <td>
                    <a asp-action="Edit" asp-route-id="@item.Id" class="btn btn-sm btn-warning">Edit</a>
                    <a asp-action="Details" asp-route-id="@item.Id" class="btn btn-sm btn-info">Details</a>
                    <a asp-action="Delete" asp-route-id="@item.Id" class="btn btn-sm btn-danger">Delete</a>
                </td>
            </tr>
        }
        </tbody>
    </table>
}

@* Loans *@
@if (Model == null || !Model.LoanDebts.Any())
{
    <p>No loan debts available.</p>
}
else
{
    <h2>Loan Debts</h2>
    <table class="table">
        <thead>
            <tr>
                <th>Debt Type</th>
                <th>Debt Amount</th>
                <th>Installments</th>
                <th>Remaining Amount</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        @foreach (var item in Model.LoanDebts)
        {
            <tr>
                <td>@item.DebtType</td>
                <td>@item.DebtAmount</td>
                <td>@item.PaidInstallments/@item.Installments</td> @* Updated for paid/total format *@
                <td>@item.RemainingAmount</td>
                <td>
                    <a asp-action="Edit" asp-route-id="@item.Id" class="btn btn-sm btn-warning">Edit</a>
                    <a asp-action="Details" asp-route-id="@item.Id" class="btn btn-sm btn-info">Details</a>
                    <a asp-action="Delete" asp-route-id="@item.Id" class="btn btn-sm btn-danger">Delete</a>
                </td>
            </tr>
        }
        </tbody>
    </table>
}

@* Monthly Rents *@
@if (Model == null || !Model.MonthlyRentsDebts.Any())
{
    <p>No monthly rent debts available.</p>
}
else
{
    <h2>Monthly Rents Debts</h2>
    <table class="table">
        <thead>
            <tr>
                <th>Debt Type</th>
                <th>Debt Amount</th>
                <th>Installments</th>
                <th>Remaining Amount</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        @foreach (var item in Model.MonthlyRentsDebts)
        {
            <tr>
                <td>@item.DebtType</td>
                <td>@item.DebtAmount</td>
                <td>@item.PaidInstallments/@item.Installments</td> @* Updated for paid/total format *@
                <td>@item.RemainingAmount</td>
                <td>
                    <a asp-action="Edit" asp-route-id="@item.Id" class="btn btn-sm btn-warning">Edit</a>
                    <a asp-action="Details" asp-route-id="@item.Id" class="btn btn-sm btn-info">Details</a>
                    <a asp-action="Delete" asp-route-id="@item.Id" class="btn btn-sm btn-danger">Delete</a>
                </td>
            </tr>
        }
        </tbody>
    </table>
}

@* Other Debts *@
@if (Model == null || !Model.OtherDebts.Any())
{
    <p>No other debts available.</p>
}
else
{
    <h2>Other Debts</h2>
    <table class="table">
        <thead>
            <tr>
                <th>Debt Type</th>
                <th>Debt Amount</th>
                <th>Installments</th>
                <th>Remaining Amount</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        @foreach (var item in Model.OtherDebts)
        {
            <tr>
                <td>@item.DebtType</td>
                <td>@item.DebtAmount</td>
                <td>@item.PaidInstallments/@item.Installments</td> @* Updated for paid/total format *@
                <td>@item.RemainingAmount</td>
                <td>
                    <a asp-action="Edit" asp-route-id="@item.Id" class="btn btn-sm btn-warning">Edit</a>
                    <a asp-action="Details" asp-route-id="@item.Id" class="btn btn-sm btn-info">Details</a>
                    <a asp-action="Delete" asp-route-id="@item.Id" class="btn btn-sm btn-danger">Delete</a>
                </td>
            </tr>
        }
        </tbody>
    </table>
}
